import { Dates, OrderItem } from "@/app/types/component-types/OrderFormData";



/**
 * 
 * @param deliveryDates Array of delivery dates selected for the order.
 * @param orderItems Array of order items added to cart by user
 * @returns An array of arrays in which the indices of the outer array map to the indices of the delivery dates, while the inner array contains the order(s) to be fulfilled on that date.
 * 
 */
export default function sortByDate(deliveryDates: Dates, orderItems: OrderItem[]) {



}